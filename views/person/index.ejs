<!--index.ejs-->
<table class="table">
        <% persons.forEach( function(model) { %>
            <tr><td><%= model.id %></td><td><%= model.name %></td><td><%= model.age %></td></tr>
        <% }); %>
    </table>